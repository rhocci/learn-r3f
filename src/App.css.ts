import { style } from '@vanilla-extract/css';

export const canvasContainer = style({
  width: '100vw',
  height: '100vh',
  background:
    'radial-gradient(circle,rgba(242, 242, 242, 1) 0%, rgba(207, 212, 227, 1) 100%)',
});
